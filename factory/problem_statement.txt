You have to build a book shop simulator which will return books for the given type.

That type could depend on the mood of the client. Our code should be like, it should take some set of parameters
and decide the type of book to be given to client. We need to expose minimal and most relevant logic to the client.